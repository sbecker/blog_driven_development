%table
  %tr
    %th Title
    %th Body
  - @posts.each do |post|
    %tr
      %td
        = post.title
      %td
        = post.body
      %td
        = #link_to "Destroy", post_path(post), :method => :delete
